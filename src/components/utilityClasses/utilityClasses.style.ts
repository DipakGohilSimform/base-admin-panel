import styled from 'styled-components'

export const ValueWrapper = styled.code`
  padding-inline: 12px;
  padding-block: 4px;
  font-size: 14px;
  border: 1px solid var(--neutral-200);
  white-space: nowrap;
  border-radius: 2px;
`
export const CodeLine = styled.code`
  color: var(--primary-500);
  font-weight: 500;
`
